services:
  - type: web
    name: watch-scanner
    runtime: python
    plan: free
    buildCommand: "pip install uv && uv sync"
    startCommand: "bash -c 'uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.0"
